<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="csrf-token" content="{{ csrfToken }}" />
    <link rel="stylesheet" href="/resources/css/deck.css" />
    <title>
      Add Flashcard
    </title>
  </head>
  <body>
    @include('partials/header')

    <div class="container">
      <h1>
        Add a Flashcard
      </h1>

      <form action="{{ route('flashcards.store', { deckId: deck.id }) }}" method="POST" class="deck-form">
        {{ csrfField() }}

        <label for="question">Question:</label>
        <input type="text" name="question" id="question" required />

        <label for="answer">Answer:</label>
        <textarea name="answer" id="answer" required></textarea>

        <div class="button-group">
          <button type="submit" class="auth-button">Add Flashcard</button>
          <a href="{{ route('decks.show', { id: deck.id }) }}" class="cancel-button">Cancel</a>
        </div>
      </form>
    </div>

    @include('partials/footer')
  </body>
</html>
